# AIの回答を貼り付けて複数の歯車を生成する

v0.6.0ではD穴・キー溝を指定する17列形式を追加しました。[軸穴の指定と例](BORE_INPUT.md)を参照してください。
v0.5.0で追加した内歯車などを混在させる12列形式も読み込めます。
最新の種類別入力・12列の例は[GEAR_TYPES.md](GEAR_TYPES.md)を参照してください。
以下の7列形式も平歯車用として引き続き読み込めます。
画面内と`examples/ai_prompt.txt`のプロンプトは17列形式を出力します。

## 操作

1. `GF Gear Batch`を開き、「AIの回答を貼り付け」を展開します。
2. 「AIに渡す定型プロンプトを表示」をオンにし、全文を選択・コピーして普段使っているAIへ貼ります。
   ファイルからコピーする場合は`examples/ai_prompt.txt`を使えます。
3. プロンプト末尾の「私の希望」を書き換えてAIに送信します。
4. AIが返したCSVをツールの「回答CSV」欄へ貼ります。
5. **「検証して一覧に取り込む」**を押します。現在の歯車一覧が置き換わります。
6. 表示された種類数・合計個数・一覧の値を確認し、配置・余白を指定して**「一括生成」**を押します。

ツールがAIへ自動送信する機能ではありません。APIキーは不要です。
AIの回答を読み込むだけでは形状を作らず、入力済みモデルも変更しません。
自然文を直接解釈せず、次の定型CSVだけを読み取ります。

## 定型CSV

```csv
name,module,teeth,pressure_angle,width,bore,quantity
Gear12,1,12,20,8,5,2
Gear20,1,20,20,8,5,4
```

この例は12歯2個＋20歯4個、合計6個を生成します。
全てモジュール1、圧力角20度、歯幅8mm、穴径5mmです。

| 項目 | 内容 |
|---|---|
| name | 名前。100文字以内。カンマ入りは二重引用符で囲む |
| module | モジュール。mm単位の正数 |
| teeth | 歯数。6～250の整数 |
| pressure_angle | 圧力角。14.5～30度 |
| width | 歯幅。mm単位の正数 |
| bore | 軸穴径。mm。穴なしは0。GFの歯底径未満 |
| quantity | 個数。1以上の整数。全行合計1000個以内 |

全7項目を毎行指定してください。空欄・単位文字・数式・「同上」は使えません。
最大200行、テキストはUTF-8で2MB以内。
CSV本体だけ、または1つの完全な `csv` コードブロックを受け付けます。
前後の解説や複数のコードブロックは取り込みエラーになります。

## 誤生成を防ぐ動作

- 取り込み前に全行の数値・個数・GF歯底径に対する穴径を検証します。
- 1行でも不正なら一覧は変更しません。表示されたエラーをAIに伝えるか手修正してください。
- 貼り付けたまま未取り込みの場合や、取り込み後に回答を変更した場合は生成を止めます。
- 「貼り付け欄を空にする」は回答だけを消し、現在の一覧を使う通常操作へ戻ります。
- 取り込み後の一覧は手編集できます。生成対象は最終的な一覧です。
- CSVには寸法を全て明記するため、共通寸法を後で変えても各行の指定が優先されます。
- 配置・余白・高速コピーは画面の設定を使います。CSVはこれらを変更しません。
- AIの計算の正しさや歯車同士の噛み合い、実用強度は保証しません。
  7列形式は平歯車用です。幾何生成時の個別失敗は従来どおり記録して後続を継続します。
